#include "NutritionCatalog.h"

NutritionCatalog::NutritionCatalog(wxWindow* parent, wxWindowID id, const wxPoint& pos, const wxSize& size, long style)
	: wxHtmlWindow(parent, id, pos, size, style)
{

}