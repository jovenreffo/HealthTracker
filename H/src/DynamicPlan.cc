#include "DynamicPlan.h"

BEGIN_EVENT_TABLE(DynamicPlan, wxPanel)

END_EVENT_TABLE()

DynamicPlan::DynamicPlan(wxWindow* parent, wxWindowID id, const wxPoint& pos, const wxSize& size, long style)
	: wxPanel{ parent, id, pos, size, style }
{
}
